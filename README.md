# Simple console game game on c++ "SNAKE"

exe works crookedly, then run it from the terminal
for that you need enter first: 'g++ Snake.cpp -o Snak', then '.\Snake'

> control in terminal:  ALT + WASD
